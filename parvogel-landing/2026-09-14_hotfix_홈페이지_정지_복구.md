# 2026-09-14 HOTFIX 홈페이지 정지 복구

## 증상
- https://parvogel.kr 백지(정지) — `Invoke-WebRequest` 시 `/parvogel/favicon.svg`, `/parvogel/assets/*.js` 요청
- 배포 로그 34800983308 성공이나, 커스텀 도메인에서 JS 404

## 원인
- `vite.config.js`에서 `base: isGhPages ? '/parvogel/' : '/'` 로 분기
- `deploy-pages.yml`은 항상 `GITHUB_PAGES=true vite build`로 빌드 → 커스텀 도메인(CNAME parvogel.kr)임에도 `/parvogel/` prefix로 빌드됨
- 결과: `dist/index.html`이 `<script src="/parvogel/assets/index-*.js">`로 생성 → https://parvogel.kr/parvogel/assets/... 404 → React 미로딩 백지
- 추가로 `src/App.jsx`에서 `BrowserRouter/HashRouter` 조건부 전환 시 hydration mismatch 가능성

## 조치 (cd1780a)
- `vite.config.js:5` `base: '/'` 고정 — HOTFIX 주석 추가
- `src/App.jsx` `HashRouter`로 원복 (조건부 제거)
- `src/pages/Landing.jsx:316` Apps Script fallback URL 복구 (env 없을 때 주문 차단 방지)
- 로컬 `npm run build` 검증: `src="/assets/index-*.js"` 확인 후 push

## 배포
- hotfix 커밋 `cd1780a` push → Actions `34801213610` build 33s + deploy 9s 성공
- 검증: `GET https://parvogel.kr/assets/index-DZht_6Pj.js 200 OK`, `href="/favicon.svg"` 정상, `WebFetch` HTML 한국 메타 정상 렌더링

## 재발 방지
- CNAME 존재 시 `base`는 항상 `/` — subpath 필요 시 별도 브랜치/환경으로 분리
- Router는 HashRouter 유지 (GitHub Pages 호환 최우선, SEO는 canonical/sitemap으로 보완)
