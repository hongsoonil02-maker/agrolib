# 2026-09-14 마케팅 팩토리 18개선 완료

> `marketing_factory/` 7대 결함 → 18개선 1~18번 전 항목 패치, `unittest` 7개 통과

## P0 즉시

### 1 UTM ?/&
`core/content_formatter.py:44` `_build_utm_links` → `urllib.parse`로 `?`/`&` 자동 분기. `smartstore .../13718496355&ut...` 404 → `?utm_source=` 정상화. drafts 7개 Naver_Blog 링크 전수 수정.

### 2 백지 영상
`core/video_maker.py:60` `drawbox`만 → `drawtext` 추가 (`top_title`/`bottom_sub` 실사용, DejaVu/맑은고딕 폰트 폴백). 검은 띠 영상 → 타이틀 입힌 9:16 정상 숏폼.

### 3 중복 스팸 차단 + 상대경로
`core/marketing_master.py:78` `_is_duplicate_today` 추가 — 같은 날짜 재실행 시 `SKIP_DUPLICATE` 종료 (force=True 우회). `2026-09-01 4중복` 재발 방지. `rendered_video` 절대경로 `C:\Users\...` → `os.path.relpath(..., FACTORY_DIR)` 상대경로 저장.

## P1

### 4 Gemini 2차 폴백
`core/content_enricher.py:37` `GEMINI_API_KEY`만 로드 → `_generate_with_gemini` 구현, OpenAI 실패 시 Gemini, 둘 다 실패 시 템플릿 3단 폴백. `requests` 기반 `generativelanguage.googleapis.com/v1beta`.

### 5 -t 45 동적
`video_maker.py:60` 고정 45초 → `_probe_duration`으로 ffprobe 조회 후 `min(45, duration)` , 9초 이하는 컷 없이 원본 유지.

### 6 유튜브 메모리
`adapters/youtube_adapter.py:79` `f.read()` 전체 적재 → 8KB 제너레이터 `_gen()` 스트리밍, timeout 180s.

### 7 인스타 v20
`adapters/instagram_adapter.py:49,94` `v18.0` → `v20.0`, `code 190` 토큰 만료 감지 후 즉시 실패 로그.

### 8 워크플로우 시크릿
`.github/workflows/marketing-factory.yml:38` `OPENAI_MAX_DAILY_COST` 추가, `Cleanup secrets` 스텝 `rm -f .env daily_cost.json`, `Upload output` → `Shorts_*.mp4`만 업로드, `timeout-minutes: 10`.

## P2

### 9 금지어
`core/content_enricher.py:124` `BANNED_PHRASES/REPLACEMENTS` + `_sanitize_narrative` — `정식허가→정식등록`, `완치 보장→완화에 도움을 줄 수 있음` 등 5종 치환. 템플릿/LLM 모두 적용.

### 10 draft_only 정렬
`core/marketing_master.py:145` `draft_only` 채널은 `ARCHIVED_DRAFT`로 고정, auto 채널은 SUCCESS/FAILED 유지. 이전 10채널 vs 8어댑터 불일치 해소.

### 11 json import
`adapters/file_draft_adapter.py:1` `import json` 누락 → `NameError` 패치.

### 12 캘린더 검증
`data/daily_calendar.json:122` Sunday `short_story_final.mp4` → 존재하는 `parvogel_case_05_eating.mp4`로 교체, `폐사율 80% 저감` → `저감에 도움을 줄 수 있음*` 법규 완화.

### 13 레이트 리밋
`adapters/telegram_adapter.py:13` `BASE_BACKOFF_SEC 30→15`, `marketing-factory.yml:14` `timeout-minutes: 10` 추가.

### 14 아티팩트 180MB
`marketing-factory.yml:66` `marketing_factory/output/` 전체 → `Shorts_*.mp4`만, `.gitignore:61` `daily_cost.json` 추가.

### 15 테스트 3→7개
`tests/test_marketing_pipeline.py:64` `test_video_maker_has_drawtext`, `test_utm_valid_url`, `test_no_absolute_windows_path`, `test_banned_phrase_sanitized` 4개 추가 — 7개 전부 통과.

### 16 관찰성
`core/marketing_master.py:160` 실패 시 `DISCORD_WEBHOOK_URL`로 `• 채널: 상태` 브로드캐스트.

### 17 스케줄러 이중
`scheduler/register_daily_task.ps1:14` `Disable-ScheduledTask` 로 기본 Disabled 등록 — GitHub Actions와 이중 실행 방지. `run_marketing_factory.bat:5` 주석 추가.

### 18 비용 가드
`core/content_enricher.py:38` `OPENAI_MAX_DAILY_COST=2.0` + `daily_cost.json` 일일 누적, 초과 시 AI 차단 후 템플릿 강제.

## 파일
```
core/content_formatter.py
core/video_maker.py
core/marketing_master.py
core/content_enricher.py
adapters/youtube_adapter.py
adapters/instagram_adapter.py
adapters/file_draft_adapter.py
adapters/telegram_adapter.py
.github/workflows/marketing-factory.yml
data/daily_calendar.json
scheduler/register_daily_task.ps1
scheduler/run_marketing_factory.bat
tests/test_marketing_pipeline.py
.gitignore
```

## 검증
- `python -m unittest marketing_factory.tests.test_marketing_pipeline -v` 7/7 OK
- `daily_calendar Sunday` video_file 존재 확인, `published_ledger` 상대경로 전환 확인
