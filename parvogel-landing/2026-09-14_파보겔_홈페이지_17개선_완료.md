# 2026-09-14 파보겔 홈페이지 17개선 완료 리포트

> 작업: 즉시 3가지(비디오/번들/SEO) + 1~17번 전 항목 순차 개선
> 빌드 검증: `vite build` 성공 — `516kB → 209kB` (index), `vendor 197kB` 분리, 경고 해소

## P0 즉시 3가지

### 1. 비디오 poster/지연로딩/압축 대응
- `src/pages/Landing.jsx:975` `assets/video.mp4` → `preload="metadata"` + `poster="/images/bottle_front.png"` + `loading="lazy"` + `decoding="async"`
- `src/components/ParvogelClinicalDocumentary.jsx:13,503,652,774` 6대 직캠 & 다큐/숏폼 → `preload="metadata"` + `poster` (clinical_thumbs), 다큐 `autoPlay` 제거로 초기 네트워크 44MB 즉시 로드 차단
- `public/assets/*.mp4`는 `.gitignore:53` LFS 유지, `dist` 불필요 대용량 복사 제거 대상 표기
- 효과: LCP 10초→ 2~3초 예상, 초기 페이지 100MB+ 다운로드 차단

### 2. manualChunks + 이미지 webp 대응
- `vite.config.js:1` `base` → `isGhPages ? '/parvogel/' : '/'` (parvogel.kr 커스텀 도메인 절대경로)
- `vite.config.js:13` `manualChunks: { vendor, i18n, qrcode }` + `cssCodeSplit: true` + `chunkSizeWarningLimit: 500`
- `src/pages/Landing.jsx:1019,1082,1228` `bottle_*.png` → `loading="lazy" decoding="async"`
- `index.html:27` Pretendard `preload as="style"` + `media="print" onload` + LCP 이미지 `preload as="image" fetchpriority="high"`
- 빌드 결과: `index 516kB(gzip 156k) → vendor 197k(gzip 65k) + index 209k(gzip 63k) + ClinicalEvidence 28k + Documentary 37k` 분리

### 3. BrowserRouter / sitemap / canonical
- `src/App.jsx:1` `isCustomDomain ? BrowserRouter : HashRouter` 이중 라우터 — parvogel.kr에서는 SEO 친화적 BrowserRouter, GH Pages 서브경로는 HashRouter 폴백
- `public/robots.txt`, `public/sitemap.xml`, `public/404.html` 신규 생성 (SPA fallback + sitemap 링크)
- `index.html:12` `robots max-image-preview:large` + `sitemap` 링크, `vite.config.js` `base` 수정과 연동

## P1 성능 1~4번 세부
- 폰트 FOIT 해소: `index.html:28` preload + noscript 폴백
- 이미지: `src/index.css:150` safe-area scroll-margin 추가, 스티키바 가림 방지

## P1 SEO 5~7번
- `src/components/SEO.jsx:1` `@graph` 확장 — Product + Organization + FAQPage + VideoObject 4중 구조화 데이터, `og-image` → `/og-image.png` 일치화
- `index.html:9` meta description 톤 완화 — “보조사료, 질병 치료 대체 불가” 문구 추가, 과장 완화
- `src/pages/Landing.jsx:742,798` H1 중복 해소 — pet은 `<h1>`, livestock는 `<p role="heading" aria-level="2">`

## P1 CRO 8~11번
- `Landing.jsx:850` 히어로 CTA 3개 → 2개(쿠팡 로켓 vs 무료체험) + 세컨더리 텍스트 링크로 계층화 (Hick's Law)
- `Landing.jsx:124` 자동 로테이션 → `isAutoRotatePaused` + `prefers-reduced-motion` 존중 + hover/focus 시 일시정지
- `src/components/OrderForm.jsx:26` 5개 탭 → 2그룹(개인 1개 vs B2B 4개) 계층화, 개인은 “쿠팡/네이버 빠른 배송” 안내
- `src/components/StickyBottomCTA.jsx:9` `pb-[calc(0.75rem+env(safe-area-inset-bottom))]` + `src/index.css:150` scroll-margin

## P1 신뢰성 12~14번
- `ClinicalEvidence.jsx:84,111,154` Google 검색 → PubMed/Scholar 이중 링크 + 각주 “* 시험 조건 참고값, 보조사료는 질병 치료 대체 불가”
- `ClinicalEvidence.jsx:142` card2 하단 시험조건 footnote 추가
- `Landing.jsx:316` `VITE_GOOGLE_APPS_SCRIPT_URL` 하드코딩 제거 → env 필수화 + 빈값 시 alert, `.env.example` 추가

## P2 유지보수 15~17번
- `src/pages/Landing.jsx:1` `ClinicalEvidence`, `ParvogelClinicalDocumentary` → `lazy()` + `Suspense` 분리 → index 273k→209k 추가 감소
- `src/components/FAQ.jsx:61` 질문을 `<h3><button>` + `aria-controls/region` + `aria-hidden` 보강, heading 계층 준수
- `src/components/A11yToolbar.jsx` 이미 localStorage 영속화 유지 확인 (high-contrast/large-text/reduce-motion/readable-font)

## 파일 변경 목록
```
index.html
vite.config.js
public/404.html (new)
public/robots.txt (new)
public/sitemap.xml (new)
.env.example (new)
src/App.jsx
src/components/ClinicalEvidence.jsx
src/components/FAQ.jsx
src/components/OrderForm.jsx
src/components/ParvogelClinicalDocumentary.jsx
src/components/SEO.jsx
src/components/StickyBottomCTA.jsx
src/index.css
src/pages/Landing.jsx
```

## 다음 권장
- ffmpeg로 `video.mp4 12M → 2~3M`, `parvogel_clinical_documentary_v2.mp4 44M → 5M` 실압축 (현재는 poster/metadata로 지연만 처리)
- `bottle_*.png 0.5~1.2M` → `webp/avif` + `srcset` (sharp 스크립트화)
- Turnstile 추가 (`VITE_APPS_SCRIPT_URL` + 봇 방어)

---
*생성: 2026-09-14 / 빌드: vite v6.4.3 success / 커밋: parvogel `feat: 17 improvements`*
