# 2026-08-30 코인봇 3계정 전면 리뷰 및 안전장치 수술 2차 (라이브 장애 봉합)

## 1. 개요 및 배경
- **작업 범위**: 코인봇 서버에서 가동 중인 3계정(Main/Alpha 20x/Free dontworry) × 4프로세스(마스터·메이저 뇌·벤처 뇌·Bot C) = 총 12개 프로세스 전면 코드 리뷰 및 결함 수정.
- **트리거**: 1차 수술(무한재진입 봉인, 거래소 SL 장착, isolated 마진, 포트 분리) 직후 재검증 요청 → 검증 과정에서 **라이브 신호 유출 장애**와 미수정·신규 결함 다수 발견.
- **커밋**: `okx_auto_trading` 저장소 `8b1ac34` — "fix: 라이브 장애 봉합 및 안전장치 보강 (2026-08-30 리뷰 2차)"

---

## 2. 🔴 라이브 장애: 클론 동기화로 인한 웹훅 포트 회귀 (신호 유출)
- **원인**: 1차 수술 시 `strategy_common.py`를 3개 디렉토리에 그대로 복사 → 계정별로 다르던 `MASTER_WEBHOOK_URL` 포트(8009/8010/8012)가 전부 **8009(Main)로 통일**됨.
- **증상**: 07:21 재시작 직후부터 20x/dontworry 뇌의 신호가 전부 Main 마스터로 전송. 클론 마스터들은 수신 0건 → 클론 계정 보유 포지션이 청산 신호조차 못 받는 관리 불능 상태.
- **유출 감사 결과**: 유출 신호는 ZEC CLOSE_LONG **1건**뿐. 재시작 포트 경합 중 우연히 20x 봇이 8013을 점유한 상태라 **올바른 계정(20x)에서 체결** — Main 계정 오염 없음 (불행 중 다행).
- **수정**:
  - `MASTER_WEBHOOK_URL`을 env 기반으로 전환 + 각 `.env`에 계정별 URL 지정
  - env 미설정 시 CRITICAL 경고 로그 + stdout 출력 (조용한 폴백 금지)
  - `BOT_C_PORT`도 동일하게 env 기반 전환 (기본 포트 통일 사고 방지)
- **교훈**: 클론 디렉토리 구조에서 "코드 파일 복사 동기화"는 계정별 설정까지 덮어쓴다. **계정별 차이는 반드시 .env로만 관리**할 것.

---

## 3. 수정 내역 (총 12건)

### 3-1. 뇌 (`strategy_common.py`)
| 항목 | 내용 |
| :--- | :--- |
| 주간 MDD 락 sticky | 발동 후 등락으로 -10% 위로 회복해도 유지, **새 주간 시작 시에만 해제** (20x 계정의 분 단위 락/해제 플래핑 해소) |
| 웹훅 URL env화 | 상기 2절 참조 |
| (1차) elif/else 봉인 | 신규 진입은 `not has_long and not has_short`일 때만, 숏 풀백 DCA는 숏 보유 중에만 — 게이트 우회·무한 재진입 원천 차단 확인 |
| (1차) 레버리지 전달 | `send_webhook(leverage=...)` → Bot C가 payload 레버리지 우선 적용 |

### 3-2. 실행기 (`bot_c_okx_swap.py`)
| 항목 | 내용 |
| :--- | :--- |
| 마진모드 tdMode | 주문 파라미터 `mgnMode`(무효) → `tdMode`로 수정, isolated 실제 적용 |
| 유령 청산 방지 | 청산 주문은 실보유 포지션의 마진모드를 조회해 추종 (기존 Cross 포지션에 isolated 청산 시 51169 "포지션 없음"이 성공으로 오인되는 것 차단) |
| set_leverage 51000 | hedge 모드 + isolated 레버리지 설정에 `posSide` 필수 파라미터 추가 |
| 심볼 안전 캡 | ccxt 심볼 파싱 수정(`LAB/USDT:USDT` → `LAB`) + 실제 클리핑 할당 + **진입에만 적용** (청산 클리핑 시 잔여 포지션 발생 방지). LAB 5,000 / EDEN 100,000 계약 |
| 51004 사후 클램프 | 한도 초과 거부 시 허용 수량으로 축소 재시도 (EDEN/LAB 반복 에러 해소) |
| clOrdId 멱등성 | 재시도 루프 전체 단일 ID + 타임아웃/일반 예외 모두 체결 조회 후 재전송 (이중 체결 원천 차단). 51004 클램프로 수량 변경 시엔 새 ID 발급 |
| SL 설치 공통화 | `_install_hard_stop()` 메서드 분리 — 일반 성공 + 멱등성 성공 경로 모두 거래소측 조건부 SL 설치, 체결가 미상 시 현재가 폴백 |

---

## 4. 남은 관찰 포인트 (미수정, 경미)
- FLIP 롱→숏 (`strategy_common.py` ~1165): 숏 레짐/HTF 게이트 없이 반대 진입 — 상승장에서 트레일링 익절 후 숏 플립 위험
- 스케일아웃(부분 청산) 시 거래소측 SL 수량 미조정 — DCA마다 SL 중복 적재
- 웹훅 HMAC에 타임스탬프/논스 없음 (리플레이 가능, 로컬호스트 바인딩이라 실위험 낮음)
- daily_analyzer 크론이 Main에만 등록 — 20x/dontworry는 auto_tune 자동 갱신 없음 (의도 여부 확인 필요)

---

## 5. 배포 및 검증 (07:54 완료)
- 3개 디렉토리 전파 + AST 문법 검증 통과
- 9개 뇌/봇 서비스 systemd 재시작 — 전부 active, 포트 8009/8010/8012/8013/8014/8016 정상 바인딩
- 3계정 6개 뇌 사이클 정상 가동, 에러 0건, URL 미설정 경고 0건
- 20x 주간 락: sticky 상태로 복원 유지, 플래핑 재발 없음

## 6. 다음 실주문 시 확인할 로그
- `⚙️ [레버리지 설정] ... (Isolated)` — isolated 실제 적용 여부
- `🛡️ [거래소 하드스탑 SL 설치 성공]` — 서버측 SL 동작 여부
